# File for storing the branch name and their corresponding features

## web-worker
- to work with web worker in react, we either create the worker file in public folder
- or import worker file as a script, created as blob